//! Cargo.lock analysis utilities — reserved for future rules.
//!
//! [`find_duplicate_deps`][super::find_duplicate_deps] is implemented in
//! `rules/mod.rs` and exposed through the top-level `rules` module.
